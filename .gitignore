# MacOS
.DS_Store

# Personal Claude overrides (not shared with team)
CLAUDE.local.md

# explicitly exposes _logs presence but ignores logs themselves
_logs/*
!_logs/.gitkeep

# explicitly exposes folder presence but ignores local documentation
!_docs/maps/.gitkeep
!_docs/context-summaries/.gitkeep
!_docs/memory/.gitkeep


subagent.log

# Ephemeral lock/marker files (batch execution)
.claude/.git-commit-lock.d/
.claude/.test-writing-mode*

# dim .git directory in the explorer
.git/

